from pathlib import Path


def run_offline_evaluation(classifier_config: Path, scheduler_config: Path) -> None:
    print(f"[pipeline] offline evaluation placeholder: {classifier_config}, {scheduler_config}")
